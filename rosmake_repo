#!/bin/bash
cd $WORKSPACE
rm -rf ros_ws
mkdir ros_ws
rosws init ./ros_ws /opt/ros/hydro
find . -type d -print0 | while IFS= read -r -d '' dir
do  
  if [ "$a" -ne "ros_ws" ] then
    rosws set "$dir" -y
  fi
done
source ros_ws/setup.bash
